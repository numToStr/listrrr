import React from "react";
import TimeIcon from "@material-ui/icons/AccessTimeOutlined";

const IconTimeCreated = props => (
    <TimeIcon fontSize="inherit" color="disabled" {...props} />
);

export default IconTimeCreated;
