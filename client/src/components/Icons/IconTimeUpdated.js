import React from "react";
import TimeLineIcon from "@material-ui/icons/TimelineTwoTone";

const IconTimeUpdated = props => (
    <TimeLineIcon fontSize="inherit" color="disabled" {...props} />
);

export default IconTimeUpdated;
